export default defineAppConfig({
  ui: {
    colors: {
      // Names the generated primary ramp; the accent actually shown flips
      // between blue and teal per theme in assets/css/main.css.
      primary: 'teal',
      neutral: 'stone'
    }
  }
})
